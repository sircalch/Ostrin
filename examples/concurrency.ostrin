fn main() -> Void {
    ch = channel<Int>()

    producer = spawn {
        for i in 1 to 5 {
            ch.send(i)
        }
        ch.close()
    }

    for value in ch {
        print(value)
    }

    task = spawn {
        3 + 4
    }
    print(task.join())
}

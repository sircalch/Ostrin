fn increment(value: Int) -> Int {
    value + 1
}

fn apply(operation: fn(Int) -> Int, value: Int) -> Int {
    operation(value)
}

fn main() -> Void {
    operation = increment
    print(operation(4))
    print(apply(operation, 9))
}

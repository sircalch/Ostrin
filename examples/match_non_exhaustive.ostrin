enum TrafficLight {
    Red
    Yellow
    Green
}

fn label(light: TrafficLight) -> String {
    match light {
        Red => "red",
        Yellow => "yellow",
        Green if true => "green",
    }
}

fn main() -> Void {
    print(label(Red))
}

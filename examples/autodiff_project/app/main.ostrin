import autodiff.autodiff
import autodiff.autodiff.{Dual}

fn poly(x: Dual) -> Dual {
    // f(x) = x^3 - 2x - 5
    x * x * x - 2.0 * x - autodiff.constant(5.0)
}

fn wave(x: Dual) -> Dual {
    // f(x) = sin(x) * exp(x / 2)
    autodiff.dsin(x) * autodiff.dexp(0.5 * x)
}

fn rosenbrock(x: Dual, y: Dual) -> Dual {
    // f(x, y) = (1 - x)^2 + 100 (y - x^2)^2
    a = (1.0 - x) * (1.0 - x)
    b = (y - x * x) * (y - x * x)
    a + 100.0 * b
}

fn recip(x: Dual) -> Dual {
    // f(x) = -(1 / x)
    -(1.0 / x)
}

fn main() -> Void {
    print(autodiff.derivative(recip, 2.0))
    print(autodiff.value(poly, 2.0))
    print(autodiff.derivative(poly, 2.0))
    print(autodiff.derivative(wave, 1.0))
    g = autodiff.gradient2(rosenbrock, 0.5, 0.5)
    print(g[0])
    print(g[1])
    print(autodiff.newton(poly, 2.0, 8))
}
